CHIP RAM512
{
    IN in[16], load, address[9];
    OUT out[16];

    PARTS:
    DMux8Way (in=load, sel=address[6..8], a=a,
                                    b=b,
                                    c=c,
                                    d=d,
                                    e=e,
                                    f=f,
                                    g=g,
                                    h=h);

    RAM64 (in=in, load=a, address=address[0..5], out=ar);
    RAM64 (in=in, load=b, address=address[0..5], out=br);
    RAM64 (in=in, load=c, address=address[0..5], out=cr);
    RAM64 (in=in, load=d, address=address[0..5], out=dr);
    RAM64 (in=in, load=e, address=address[0..5], out=er);
    RAM64 (in=in, load=f, address=address[0..5], out=fr);
    RAM64 (in=in, load=g, address=address[0..5], out=gr);
    RAM64 (in=in, load=h, address=address[0..5], out=hr);

    Mux8Way16 (a=ar,
               b=br,
               c=cr,
               d=dr,
               e=er,
               f=fr,
               g=gr,
               h=hr, sel=address[6..8], out=out);
}
