CHIP RAM64
{
    IN in[16], load, address[6];
    OUT out[16];

    PARTS:
    DMux8Way (in=load, sel=address[3..5], a=a,
                                    b=b,
                                    c=c,
                                    d=d,
                                    e=e,
                                    f=f,
                                    g=g,
                                    h=h);

    RAM8 (in=in, load=a, address=address[0..2], out=ar);
    RAM8 (in=in, load=b, address=address[0..2], out=br);
    RAM8 (in=in, load=c, address=address[0..2], out=cr);
    RAM8 (in=in, load=d, address=address[0..2], out=dr);
    RAM8 (in=in, load=e, address=address[0..2], out=er);
    RAM8 (in=in, load=f, address=address[0..2], out=fr);
    RAM8 (in=in, load=g, address=address[0..2], out=gr);
    RAM8 (in=in, load=h, address=address[0..2], out=hr);

    Mux8Way16 (a=ar,
               b=br,
               c=cr,
               d=dr,
               e=er,
               f=fr,
               g=gr,
               h=hr, sel=address[3..5], out=out);
}