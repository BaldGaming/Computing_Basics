CHIP RAM8
{
    IN in[16], load, address[3];
    OUT out[16];

    PARTS:
    DMux8Way (in=load, sel=address[0], out=a0,b0,c0,d0,e0,f0,g0,h0);
    DMux8Way (in=load, sel=address[1], out=a1,b1,c1,d1,e1,f1,g1,h1);
    Register (in=);
    Mux8Way16 ();
}
